{
  "name": "bitween-expo-eyeballs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prep": "node scripts/prep-assets.mjs",
    "dev": "npm run prep && vite",
    "test": "node scripts/prep-assets.mjs --selftest && node src/eyes/gaze.selftest.ts && node src/face/mapping.selftest.ts",
    "prebuild": "npm run prep",
    "build": "npm test && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "pngjs": "^7.0.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1"
  }
}
